{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
<h1>Créer un compte {{ type }}</h1>

    {{ form_start(registrationForm) }}
        {{form(registrationForm)}}
    {{ form_end(registrationForm) }}

{% endblock %}