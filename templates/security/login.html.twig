{% extends 'base.html.twig' %}

{% block title %}Log in!
{% endblock %}

{% block body %}
<div class="row">
<div class="col-md-6 p-3 mt-3 border-end border-white">
	<h2>Bienvenue</h2>
	<p>Meet-my-Dev est une application dédiée à la rencontre entre entreprises & développeurs
		Vous êtes une entreprise à la recherche de votre développeur ?
		Ou un développeur en quête de votre entreprise ?
		<br><br>Inscrivez-vous pour decouvrir tous les profils !
	</p>
</div>
	<div class="d-flex justify-content-center p-3 mt-3 col-md-6">
		<form method="post" id="loginForm" class="border p-3 rounded-3">
			{% if error %}
				<div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
			{% endif %}

			<h1 class="h3 mb-3 font-weight-normal">Please sign in</h1>
			<label for="inputUsername">Username</label>
			<input type="text" value="{{ last_username }}" name="username" id="inputUsername" class="form-control" autocomplete="username" required autofocus>
			<label for="inputPassword" class="mt-3">Password</label>
			<input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>

			<input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

			{#
			        Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
			        See https://symfony.com/doc/current/security/remember_me.html
			
			        <div class="checkbox mb-3">
			            <label>
			                <input type="checkbox" name="_remember_me"> Remember me
			            </label>
			        </div>
			    #}

			<button class="btn btn-lg btn-light mt-3" type="submit">
				Sign in
			</button>
            <p class="p-3">You don't have any account ? Create one below !</p>
            <div class="d-flex justify-content-between">
                <a href="/registerDev" class="register_link bg-light p-2 rounded-3 text-black bold">I am a developer</a>
                <a href="/registerCompany" class="register_link bg-light p-2 rounded-3 text-black bold">I am a company</a>
            </div>
		</form>  
	</div>
</div>
{% endblock %}
